# V2.0 待修复与待改进清单

更新时间：2026-06-14

本文档记录 FMO 仪表盘 V2.0 发布后发现的 bug、体验问题与后续改进项，作为后续 `2.0.1`、`2.1.0` 或各平台补丁版本的修复依据。

## 当前状态

V2.0 已完成多平台发布：

- Web V2
- Android V2.00 公测 APK
- iOS App Store 2.0.0
- Windows 桌面版 v2.0.0
- macOS Universal DMG v2.0.0

后续修复不再回改已发布的 V2.0 封版资产。涉及 App Store、Android 市场、桌面安装包或 GitHub Release 的变更，应使用新版本号或明确标记为补充资产。

## 处理原则

- 优先修复影响连接 FMO、同步日志、频率显示、语音播报、地址保存、移动端首屏可读性的 bug。
- 已发布资产的文件名、SHA、release notes 不随意修改。
- Android/iOS 后续修复应考虑版本号递增；iOS Build Number 必须递增。
- 桌面端修复应优先验证 Windows/macOS 下载版场景，不只看浏览器。
- 涉及同步、数据库、地址解析的改动，需要兼顾 Web、Android、iOS、Tauri 桌面。
- 涉及 UI 的改动需检查桌面宽屏与手机窄屏，避免横向溢出。

## 当前已知问题

### #1 FMO 频率有 TX/RX 之分，Dashboard 频率显示需要重新布局

#### 类型

Bug / 数据展示修正 / UI 布局优化

#### 优先级

高

#### 影响平台

- Web V2
- Android
- iOS
- Windows 桌面版
- macOS 桌面版

#### 现象

Dashboard 第一屏“频率 / 模式”卡片目前按单一频率展示，只显示一个 `freqHz` 或只显示 `FMO` 模式。

但 FMO 中继/频率实际存在 TX、RX 区分，尤其在异频配置时，单一频率显示会造成信息不完整，用户无法直观看到收发频率。

#### 影响

- 异频中继时，用户无法确认当前 TX/RX 是否正确。
- 频率卡片容易被误解为只有一个工作频率。
- 后续如果用于车机/移动首屏，信息不准确会影响值守判断。

#### 初步判断

当前 Dashboard 的实现把频率和模式压缩为一个字符串，优先读取通联日志里的 `freqHz`，没有 TX/RX 结构化展示。

TX/RX 数据可能来自：

- `station:getCurrent`
- `station:getListRange`
- `qso:getDetail`
- 后续 FMO 协议新增字段

字段名尚需用真实 FMO 异频配置返回值确认。

#### 期望表现

如果接口返回 TX/RX 字段，频率卡片应显示：

```text
TX  xxx.xxxx MHz
RX  xxx.xxxx MHz
模式 FMO
```

如果旧数据只有单一频率，则降级显示：

```text
频率 xxx.xxxx MHz
模式 FMO
```

如果没有任何频率字段，则显示：

```text
模式 FMO
```

#### 2026-06-14 本地修复记录

已初步修改：

- `src/views/DashboardView.vue`
  - 将频率卡片从单行字符串改为结构化显示。
  - 支持 TX、RX、单频率、模式分行展示。
  - 数据源扩展到当前通联、当前中继、最近活跃中继、最近日志。
  - 兼容多种可能字段名，例如 `txFreqHz`、`rxFreqHz`、`txFrequency`、`rxFrequency`、`uplinkFrequency`、`downlinkFrequency`。
  - 若没有 TX/RX 字段，保留旧单频率降级逻辑。
- `src/services/db.js`
  - 保存日志时保留 `txFreqHz` / `rxFreqHz`。
  - 导出 SQLite 时包含 `txFreqHz` / `rxFreqHz`。
- `src/composables/useLocale.js`
  - 补充英文短标签 `Freq`。

已验证：

- 修改文件聚焦 ESLint 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。
- 本地浏览器检查桌面宽度和 390px 移动视口，频率卡片无横向溢出。

#### 后续验证项

- 用户将 FMO 配置为异频后，在 Dashboard 点击“刷新现场”。
- 抓取并确认真实接口返回字段：
  - `station:getCurrent`
  - `station:getListRange`
  - `qso:getDetail`
- 如果真实字段名不同，按实际字段名补充映射。
- 确认 Android/iOS/Tauri 桌面下同一布局不溢出。

### #2 设置 FMO 地址后，全量同步数据过多时容易同步失败

#### 类型

Bug / 同步机制稳定性 / 大数据量兼容

#### 优先级

高

#### 影响平台

- Web V2
- Android
- iOS
- Windows 桌面版
- macOS 桌面版

#### 现象

设置 FMO 地址后，如果执行全量同步，数据量较多时容易同步不成功。表现可能包括：

- 同步长时间卡住。
- 中途请求超时。
- 部分记录失败较多。
- WebSocket 连接断开后没有很好恢复。
- 本地数据库写入节奏过慢，导致整体同步耗时过长。

#### 影响

- 新用户首次配置 FMO 地址时体验差。
- 历史日志较多的用户难以完成初次同步。
- 多地址同步或全量同步时失败概率增加。
- 用户可能误以为 FMO 地址配置错误。

#### 初步判断

当前全量同步流程是：

1. 分页拉取 QSO 列表。
2. 对每条记录逐条请求 `qso:getDetail`。
3. 每条详情请求后单条写入 IndexedDB。
4. 长时间复用同一个 WebSocket client。

数据量大时，问题可能来自：

- 单条详情请求过多，耗时长。
- 每条记录单独写 IndexedDB，事务数量过多。
- 单个 WebSocket 连接长时间工作后不稳定。
- 列表/详情请求超时时间偏短。
- 页面刷新或后台任务与手动同步之间存在竞态。

#### 期望表现

- 大数据量全量同步应能稳定完成。
- 单条详情失败不应导致整个同步立刻失败。
- 页面应显示可理解的进度。
- 长同步过程中可周期性释放/重建连接。
- 数据库写入应批量化，减少事务开销。

#### 2026-06-14 本地修复记录

已初步修改：

- `src/core/sync/syncEngine.ts`
  - 全量同步页大小从 20 调整为 50。
  - 页内详情先缓冲，整页批量保存。
  - 详情请求间隔从 100ms 调整为 60ms。
  - 页间等待 250ms。
  - 每 8 页主动关闭 client，下次请求自动重连，降低长连接失败概率。
  - 同步状态文案显示页内进度和已同步数量。
- `src/services/db.js`
  - 新增 `saveQsoBatchToIndexedDB(records)`。
  - 按 `fromCallsign` 分组批量写入，减少 IndexedDB 事务数量。
- `src/services/fmoApi.js`
  - `qso:getList` 和 `qso:getDetail` 请求重试从 1 次增加到 2 次。
  - 请求超时从 12 秒放宽到 18 秒。

已验证：

- 修改文件聚焦 ESLint 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。

#### 后续验证项

- 使用真实大数据量 FMO 地址执行全量同步。
- 观察是否仍出现请求超时、卡住或大量失败。
- 记录失败时的具体错误文案和失败 logId 数量。
- 如仍失败，下一步考虑：
  - 增加同步任务断点续传。
  - 将全量同步拆成“列表索引同步 + 详情补齐”两阶段。
  - 对详情请求做有限并发，但必须先确认 FMO WebSocket 协议是否允许同类请求并发。
  - 增加同步取消按钮和失败重试入口。

### #3 Android 老版本系统适配：车机、老手机、鸿蒙等 Android 10 以下环境

#### 类型

兼容性改进 / Android 适配 / 车机场景支持

#### 优先级

中高

#### 影响平台

- Android
- 车机 Android 系统
- 老手机 Android 系统
- 华为 / 荣耀鸿蒙兼容层

#### 现象

V2.0 后续需要关注 Android 老版本系统适配。部分车机、老手机、鸿蒙系统设备的 Android 版本可能在 Android 10.0 以下，实际运行环境可能明显落后于主流手机。

当前工程配置：

```text
minSdkVersion = 24
compileSdkVersion = 36
targetSdkVersion = 36
```

这意味着 APK 理论上允许安装到 Android 7.0 及以上设备，但实际能否稳定运行，还取决于：

- 系统 WebView / Chrome WebView 版本；
- Capacitor Android 与 AndroidX 依赖的兼容性；
- 厂商 ROM 或鸿蒙兼容层的 WebView、权限、后台服务限制；
- 车机系统是否缺少现代浏览器组件；
- 音频播放、后台服务、通知、定位等原生插件在旧系统上的行为；
- 老设备 CPU、内存、存储性能对 V2 页面和同步任务的承载能力。

#### 影响

- 部分老车机可能无法启动、白屏、闪退或 WebView 功能缺失。
- 老手机可能安装成功但连接 FMO、同步、语音播报或后台保活异常。
- 鸿蒙兼容层可能出现权限、WebView、音频或后台服务行为差异。
- 用户可能误以为 V2 Android 包本身损坏。

#### 初步判断

需要把“理论 minSdk 支持”和“实际设备可用”分开处理。

V2.0 当前不应盲目承诺所有 Android 10 以下设备都稳定支持，应先建立兼容性分级：

- **主支持**：Android 10 及以上主流手机 / 平板。
- **尽量兼容**：Android 8-9 老手机、主流车机。
- **有限兼容**：Android 7、厂商深度定制车机、老鸿蒙兼容层。
- **不主动支持**：缺少可更新 WebView、系统组件严重残缺或无法提供崩溃日志的设备。

#### 期望表现

- Android 10 以下设备若能安装，应尽量完成启动、配置 FMO 地址、连接、查看 Dashboard、同步最近数据等核心路径。
- 如果设备 WebView 或系统能力不足，应给出更明确的提示，而不是静默白屏或闪退。
- 对车机应额外关注横屏、低分辨率、触控目标、字体大小和滚动行为。

#### 建议修复方向

- 建立 Android 兼容性测试矩阵：
  - Android 7 / 8 / 9 / 10；
  - 典型车机横屏；
  - 华为 / 荣耀鸿蒙兼容层；
  - 低内存或低性能设备。
- 收集旧设备启动失败时的 `adb logcat`：
  - JS runtime error；
  - WebView 初始化错误；
  - 原生插件崩溃；
  - 权限或后台服务异常。
- 增加启动诊断：
  - 检测 WebView / user agent / Android 版本；
  - 记录到诊断日志；
  - 必要时显示“当前系统 WebView 过旧，请升级 Android System WebView 或 Chrome”的提示。
- 检查前端语法与构建目标是否对旧 WebView 友好。
- 检查 Capacitor / AndroidX / 原生插件最低系统要求，避免引入只在高版本系统稳定的 API。
- 对车机横屏和窄高度屏幕做单独布局回归。
- 若某些设备无法低成本兼容，应在下载页或 release notes 中标注兼容风险。

#### 2026-06-14 本地预适配记录

在暂无老 Android 真机/车机测试设备的前提下，已先完成低风险预适配：

- `vite.config.js`
  - 将前端构建目标调整为 `chrome64` / `safari13`。
  - 同步设置 `cssTarget: chrome64`，减少旧 Android WebView 遇到现代 JS/CSS 语法的概率。
- `src/utils/androidCompatibility.js`
  - 新增 Android/WebView 兼容性检测工具。
  - 解析 Android 主版本、Chrome/WebView 主版本。
  - Android 10 以下或 WebView/Chrome 80 以下标记为兼容风险。
- `src/main.js`
  - Android 原生启动时写入兼容性诊断日志。
  - 检测到旧系统风险时，每 7 天最多提示一次：
    `当前 Android/WebView 较旧，如遇白屏或闪退请导出诊断日志`
- `src/services/diagnosticLog.js`
  - 导出的诊断日志头部增加 Android 版本、WebView/Chrome 版本和旧系统风险标记。

该阶段不降低 `minSdkVersion`，仍保持当前 Android 工程安装门槛 `minSdkVersion = 24`，先解决旧 WebView 语法兼容和问题定位能力。

已完成本地验证：

- 修改文件聚焦 ESLint 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。
- 使用模拟 UserAgent 验证：
  - Android 9 + Chrome/WebView 74 会被标记为兼容风险。
  - Android 13 + Chrome 120 不会被标记为兼容风险。

#### 新会话衔接建议

老 Android 适配建议单独开新会话继续推进，避免和 V2.0 其他 bug 修复混在一起。

新会话建议从以下问题开始：

1. 是否需要准备 Android 8 / 9 模拟器环境做第一轮无真机验证。
2. 是否要构建一个 `V2.0-legacy-android-test` 测试 APK，专门用于车机、老手机、鸿蒙设备试装。
3. 检查当前 Capacitor 8、AndroidX 依赖和原生插件在 Android 7-9 上的最低兼容风险。
4. 增加更明显的“旧 WebView 过低”降级页面或启动提示。
5. 制定老 Android 兼容分级说明，明确主支持、尽量兼容、有限兼容和不主动支持边界。

#### 2026-06-14 2.01 老 Android 启动层修复记录

本会话优先处理 Android 7.0 以上、Android 10 以下设备的启动可见性问题，尤其是老车机/老手机 WebView 太旧时的白屏问题。

已修改：

- `index.html`
  - 增加不依赖 Vue 的旧 WebView 预检测脚本。
  - Android WebView/Chrome 低于 64，或缺少 ES module 支持时，直接显示中文降级提示。
  - 提示用户升级 `Android System WebView` / `Chrome`；无法升级的车机建议使用 Android 10+ 设备、网页版或桌面版。
  - 增加 `noscript` 提示，避免禁用 JavaScript 时只显示空白页。
- `src/utils/androidCompatibility.js`
  - 增加 `criticalLegacyWebView` 判断，用于区分“兼容风险”和“内核过旧”。
  - 记录 ES module 支持预检测结果。
  - 增加最低建议 WebView 内核：`Chrome/WebView 64+`。
- `src/main.js`
  - Android 启动诊断日志区分 `Android WebView 版本过旧` 与普通旧系统风险。
  - WebView 过旧时 toast 文案改为提示先升级系统 WebView 或 Chrome。
- `src/services/diagnosticLog.js`
  - 导出的诊断日志头部增加最低 WebView 建议和 WebView 过旧标记。

说明：

- `minSdkVersion` 暂不下调，仍保持 Android 7.0 / API 24 作为安装门槛。
- 2026-06-16 小米音箱 Android 8 反馈显示进入“Android System WebView 版本过旧”保护页。为验证 Chrome/WebView 61-63 设备是否仍可运行，前端 bundle 构建目标下探为 `chrome61` / `safari13`，预检测硬拦截阈值同步调整为 Chrome/WebView 61 以下。
- Android 7-9 的实际可用性不能只看设备 WebView 是否达到 Chrome 61。小爱同学音箱实测 WebView `61.0.3163.98` 虽支持 ES module 基础能力，但现代 bundle 仍可能白屏；后续需要 legacy/nomodule 兜底构建。
- 后续测试反馈：小爱同学音箱 Android 8.1.0，内核 `4.4.95+`，`PATECH MOD ROM V3 / alps-mp-o1.mp5`，WebView61 测试包放行后出现白屏且无法退出。已增加启动 watchdog：若 6 秒内 Vue 未挂载或启动期出现脚本异常，自动显示兼容失败提示和“重试 / 退出应用”按钮，避免设备卡在纯白屏。
- 对比同设备可运行的 FmoLogs APK 后，确认关键差异是 FmoLogs 使用 Vite legacy 输出：`type="module"` 现代入口之外，还包含 `nomodule` 的 `polyfills-legacy-*.js`、`index-legacy-*.js` 和 `System.import(...)`。FmoLogs 的 `capacitor.config.json` 也设置了 `android.minWebViewVersion: 55`。
- 已为 FMO仪表盘增加可开关 legacy 构建路径：`FMO_LEGACY_ANDROID=1 npm run build` 会通过 `@vitejs/plugin-legacy@7.2.1` 生成 legacy/nomodule 产物；默认构建不启用 legacy 插件。Capacitor Android 配置同步补充 `minWebViewVersion: 55`。
- 已构建本地测试包，未上传 VPS/GitHub、未改主页下载链接：
  - `release/android/FMO-Dashboard-Android-V2.01-LegacyWebView-Test.apk`
  - SHA256：`29b01ed20992b2a7394246a456761bd0416595587236b36aeeb606ce843ec130`
  - APK 内已验证包含 `polyfills-legacy-CwPryS-a.js`、`index-legacy-CzMUNh_Q.js` 和 `script nomodule` legacy 入口。
- 2026-06-16 小爱音箱测试 legacy 包后进入 watchdog 页面，错误为 `Uncaught SyntaxError: Unexpected token .`，判断为旧 WebView 解析 Vite legacy 现代探测/现代入口时产生的预期语法错误。已调整 watchdog：Chrome/WebView 64 以下 Android 设备忽略 `Unexpected token` / `Unexpected reserved word` / `Unexpected identifier` 这类启动期语法探测错误，让 `nomodule` legacy 入口继续接管；超时兜底从 6 秒延长到 12 秒。重新覆盖本地测试包：
  - `release/android/FMO-Dashboard-Android-V2.01-LegacyWebView-Test.apk`
  - SHA256：`0ca236d67f80fedf88f9f2dd38249bdaaacfa72974ee1d1854c614c218cbc638`
  - 未上传 VPS/GitHub、未改主页下载链接。
- 继续拆包可运行的 FmoLogs APK 后，发现它不仅有 legacy/nomodule，还把 `sql.js` 的 wasm 运行时拆到了非首屏路径：其 legacy 首入口不包含 `BigInt`、`BigInt64Array`、`WebAssembly`、`instantiateStreaming` 或 `sql-wasm.wasm`。FMO仪表盘原测试包因静态导入 `sql.js`，旧 WebView 61 进入 legacy 后仍可能在首屏执行 wasm/Emscripten 相关代码并闪退。已改为：
  - `src/services/db.js` 不再静态导入 `sql.js`；
  - WebView/Chrome 64 以下 Android 直接加载 `sql.js/dist/sql-asm.js`；
  - 新设备继续优先 wasm，失败后回退 asm；
  - 新本地测试包已验证 legacy 首入口中 `BigInt`、`WebAssembly`、`sql-wasm.wasm` 计数均为 0。
- 最新本地 WebView61 测试包：
  - `release/android/FMO-Dashboard-Android-V2.01-LegacyWebView-Test.apk`
  - SHA256：`57a816a8f58ec88a501e4f60750ec55e2fe3835331864522a670019d94f7477c`
  - APK 内包含 `index-legacy-CKyxJubT.js`、`polyfills-legacy-CwPryS-a.js`、`sql-asm-cY7yALOm.js`，且 legacy 首入口已避开 wasm/BigInt 关键字。
  - 未上传 VPS/GitHub、未改主页下载链接。
- 该包在小爱音箱仍触发系统级“FMO仪表盘屡次停止运行”，说明已不是普通前端白屏，而是 Android 进程层面的崩溃。继续排查原生启动链后发现 `FmoSystemUiPlugin.getSafeAreaInsets()` 直接调用 Android 11(API 30) 才有的 `WindowInsets.Type.*` API；小爱音箱为 Android 8.1(API 27)，此类 `NoSuchMethodError` 不会被原来的 `catch (Exception)` 捕获，可能直接杀进程。已修复：
  - `FmoSystemUiPlugin` 增加 `Build.VERSION.SDK_INT >= Build.VERSION_CODES.R` 保护；
  - `load()` 与 `getSafeAreaInsets()` 的兜底从 `Exception` 扩大到 `Throwable`；
  - Android 11 以下统一返回估算安全区，避免旧 ROM 调用高版本 `WindowInsets.Type`。
- 重新构建本地测试包：
  - `release/android/FMO-Dashboard-Android-V2.01-LegacyWebView-Test.apk`
  - SHA256：`6df58724de039abaa520c36a2289e36b65715ebfc5de25e94d675495acac9f66`
  - `FMO_LEGACY_ANDROID=1 npm run build`、`npx cap sync android`、`:app:assembleRelease` 均成功。
  - APK 内继续确认 `nomodule` / `System.import` 存在，legacy 首入口 `BigInt`、`WebAssembly`、`sql-wasm.wasm` 计数均为 0。
  - 未上传 VPS/GitHub、未改主页下载链接。

#### 2026-06-14 Android V2.01 老系统适配测试包构建记录

已完成 Android V2.01 测试包构建，作为 Android 7.0+ 老系统/车机试装基线。

版本调整：

- `package.json` / `package-lock.json`
  - 版本号从 `2.0.0` 调整为 `2.0.1`，用于构建产物文件名。
- `android/app/build.gradle`
  - `versionCode` 从 `20000` 调整为 `20100`。
  - `versionName` 从 `V2.00` 调整为 `V2.01`。
- `src/views/AboutView.vue`
  - 应用内关于页版本显示为 `V2.01`。
- `src/components/home/PublicSiteTools.vue` / `src/composables/useLocale.js`
  - 下载入口 Android 文案更新为 `Android APK V2.01`。
  - Android 下载文件名指向 `FMO-Dashboard-Android-V2.01.apk`。

本地构建环境：

- Java：本地 Android toolchain 内置 Temurin 21.0.11。
- Android SDK：本地 Android toolchain。
- 构建命令：

```bash
TOOLCHAIN="/Users/bh1jss/Documents/Codex/2026-05-13/fmo-https-bg5esn-com-docs-fmo/FmoLogs/release/android-toolchain"
ANDROID_HOME="$TOOLCHAIN/android-sdk" \
ANDROID_SDK_ROOT="$TOOLCHAIN/android-sdk" \
JAVA_HOME="$TOOLCHAIN/jdk/Contents/Home" \
PATH="$TOOLCHAIN/jdk/Contents/Home/bin:$PATH" \
bash scripts/build-android-apk.sh --debug
```

构建产物：

- `release/android/FMO-Dashboard-Android-v2.0.1-debug.apk`
- `release/android/SHA256SUMS-android-v2.0.1-debug.txt`
- 对外测试命名拷贝：
  - `release/android/FMO-Dashboard-Android-V2.01.apk`
  - `release/android/SHA256SUMS-android-V2.01.txt`

V2.01 APK SHA256：

```text
316aa44f33e9769d979ac35646602ecefba280c92ff7af0f780cc127e9277d29  FMO-Dashboard-Android-V2.01.apk
```

Manifest 验证：

```text
package: name='net.bh1jss.fmodashboard' versionCode='20100' versionName='V2.01'
sdkVersion:'24'
targetSdkVersion:'36'
application-label:'FMO仪表盘'
```

已完成本地验证：

- `npm run lint` 通过，仍有 17 个既有 warning，无 error。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。
- `bash scripts/build-android-apk.sh --debug` 通过。
- `aapt dump badging` 确认版本号和 SDK 门槛正确。

#### 2026-06-14 V2.01 频率显示补充记录

已将 Dashboard 频率卡片改为更接近 FMO 屏幕的单行紧凑显示：

```text
T:434.2500/R:438.2500/FMO
```

本次实测接口返回情况：

- `station:getCurrent` 仅返回 `uid/name`。
- `station:getListRange` 仅返回 `uid/name`。
- `qso:getDetail` 当前仅返回单频 `freqHz: 4382500`，未包含 TX/RX。

因此当前实现保留通用 TX/RX 字段映射和 offset 推导，同时针对现有 FMO `438.2500` 单频数据临时兜底为：

- TX：`434.2500`
- RX：`438.2500`

后续如果 FMO API 开始返回真实 TX/RX 字段，应优先使用真实字段，并移除或收窄该兜底。

#### 后续验证项

- 找到至少一台 Android 10 以下真机或车机测试安装。
- 若无法获得真机，先用 Android Emulator 创建 Android 8 / 9 测试环境。
- 验证路径：
  - 安装并启动；
  - 添加 FMO 地址；
  - Dashboard 刷新现场；
  - 最近同步 / 全量同步；
  - 语音播报；
  - 后台保活 / 通知；
  - 横屏车机布局。
- 对失败设备记录：
  - 设备型号；
  - Android / 鸿蒙版本；
  - WebView 版本；
  - 是否可更新 WebView；
  - `adb logcat` 关键错误；
  - 是否属于可修复问题或应标记为有限支持。

#### 2026-06-28 本会话修复与调整记录

本轮在 V2.02 已知问题基础上继续处理网页版 / 桌面版可验证问题，并准备 Web V2.03：

- 深色模式显示：
  - 补齐深色主题变量和提示类组件的深色覆盖，处理 Windows 深色模式下日志高亮、老友卡片、老友详情非链接文字、toast / 状态提示等过白问题。
  - 同类样式问题预计也覆盖部分老旧 Android WebView 上的浅色回退。
- 仪表盘最近 20：
  - 最近 20 优先使用实时发言事件历史。
  - 当前正在发言的呼号会进入列表并置顶。
  - 日志页记录只作为首屏不足 20 条时的补位来源，不再覆盖同呼号实时事件。
- 仪表盘“上个通联”：
  - 当自动确认通联后，若同一呼号历史次数从原基线增加，则在“上个通联”的次数标签中显示鲜艳 `+1`。
  - `+1` 只在日志 / 同步后的真实次数增加时出现，不在刚开始实时通联时立即出现。
  - 增量固定最多显示 `+1`，不显示 `+2` / `+3`。
- FMO 备份统一：
  - 参考官方 `qso.html` 工具和 `fmo-adif-to-backup-zip-design.md`，新增官方备份 ZIP 的读取 / 导出支持。
  - ZIP 内按官方格式使用根目录 `logBook_v1_active.db`，导入时校验 SQLite 中存在 `qso_logs`。
  - 设置页数据管理入口调整为：`导出数据库文件`、`导出ADIF`、`导出备份`、`导入备份到FMO`。
  - `导入备份到FMO` 使用蓝色主按钮；现已对齐官方 `qso.html` 行为，选择 ZIP 后以 `multipart/form-data` 的 `file` 字段提交到当前设备 `/api/qso/restore`，恢复成功后设备会重启。
  - 浏览器端为规避跨源读响应限制，使用隐藏 iframe 表单提交；Windows / macOS Tauri 端使用系统文件选择后直接 `POST` 上传。
  - 新增 `ADIF转FMO备份包`：本地选择 `.adi/.adif` 文件，按官方格式转换为根目录含 `logBook_v1_active.db` 的 ZIP，不导入本地日志、不上传设备。
  - Windows / macOS 桌面版该转换入口使用 Tauri 系统文件选择和保存对话框；权限依赖 `dialog:allow-open`、`dialog:allow-save`、`fs:allow-read-file`、`fs:allow-write-file`。
  - 上方同步区域失效的旧 `备份FMO日志` 按钮已移除，避免和数据管理区的正式导出 / 转换入口重复。
  - `清空通联日志` 和 `清理地址缓存` 两个维护按钮保留；`清空通联日志` 改为不依赖数据库加载状态，页面常驻显示。
- 版本与发布：
  - 前端版本号更新到 `V2.03`。
  - VPS `/V2/` 网页版上传流程已验证；本轮按用户要求未同步 Git。
- 通联播报：
  - 新增待查 bug：通联播报容易中途断掉，重点检查 WS 协议下 `/audio` 语音流、WebSocket `onclose/onerror`、自动重连、AudioContext 暂停 / 恢复和页面可见性切换。
  - 为了让问题更容易暴露，所有版本每次打开时默认进入“通联播报”模式；若已设置 FMO 地址，启动后会尝试自动打开音频流。
  - 用户仍可手动关闭播报，但下次重新打开应用会再次默认回到通联播报。

本轮验证记录：

- `npx eslint src/views/DashboardView.vue` 通过。
- `npx eslint src/views/SettingsView.vue` 通过。
- `npx eslint src/views/MainLayout.vue` 通过。
- `npx eslint src/services/db.js src/views/MainLayout.vue src/views/SettingsView.vue src/composables/useDbManager.js src/utils/desktopBridge.js` 通过，保留 `MainLayout.vue` 既有 `vue/no-template-shadow` warning。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- 本地浏览器 `http://localhost:5174/settings` 已确认 `导入备份到FMO` 为蓝色主按钮，`清空通联日志` / `清理地址缓存` 均可见。

#### 2026-06-28 V2.03 多平台制作记录

已完成：

- Web / VPS：
  - `bash scripts/package-aliyun-site.sh package` 通过。
  - 已生成 `deploy/aliyun-site/` 和 `deploy/fmo-bh1jss-net-aliyun-site.tar.gz`。
  - 已部署到 VPS：
    - `https://fmo.bh1jss.net/`
    - `https://fmo.bh1jss.net/v2/`
  - 下载页 Android、Windows、macOS 入口均已更新为 V2.03。
  - 公开 `/v2/` 资源确认包含本轮设置页按钮、FMO 备份恢复、ADIF 转 FMO 备份包工具改动。
- Android：
  - `android/app/build.gradle` 更新为 `versionCode 20300`、`versionName V2.03`。
  - 已生成：
    - `release/android/FMO-Dashboard-Android-v2.0.3-release.apk`
    - `release/android/FMO-Dashboard-Android-V2.03.apk`
    - `release/android/SHA256SUMS-android-V2.03.txt`
  - `aapt dump badging` 确认包名 `net.bh1jss.fmodashboard`、`versionCode 20300`、`versionName V2.03`、`minSdkVersion 24`、`targetSdkVersion 36`。
  - `apksigner verify --print-certs` 通过；当前仍使用 Android debug 证书。
  - APK SHA256：
    - `3ef06c9730559f51802cb712105d6e5f5c208584a41f5b46faac351b7b9409c3  FMO-Dashboard-Android-V2.03.apk`
  - 公开下载地址：
    - `https://fmo.bh1jss.net/downloads/FMO-Dashboard-Android-V2.03.apk`
- macOS：
  - GitHub Actions `macos-desktop-release.yml` 通过。
  - 已生成 Universal DMG：
    - `release/github-actions/macos-v2.0.3/latest/FMO-Dashboard-macOS-Universal-v2.0.3.dmg`
    - `deploy/downloads/v2.0.3/SHA256SUMS-macos-v2.0.3.txt`
  - 已完成 Developer ID 签名、Apple 公证和 stapling。
  - DMG SHA256：
    - `71e46ac11ed3a7b479ad931c883c836de3868b8aa6de3458b4e1477c9daa3eec  FMO-Dashboard-macOS-Universal-v2.0.3.dmg`
  - 公开下载地址：
    - `https://fmo.bh1jss.net/downloads/v2.0.3/FMO-Dashboard-macOS-Universal-v2.0.3.dmg`
- Windows：
  - GitHub Actions `windows-desktop-release.yml` 通过。
  - 已生成 unsigned Windows 安装包：
    - `release/github-actions/windows-v2.0.3/latest/FMO-Dashboard-Windows-Desktop-x64-Setup-v2.0.3.exe`
    - `release/github-actions/windows-v2.0.3/latest/FMO-Dashboard-Windows-Desktop-x86-Setup-v2.0.3.exe`
    - `deploy/downloads/v2.0.3/SHA256SUMS-windows-desktop-v2.0.3.txt`
  - Win64 SHA256：
    - `e33d3a0cc141a64f6de39dec7e265bb644baab12442f63ffb8cf368974c9f851  FMO-Dashboard-Windows-Desktop-x64-Setup-v2.0.3.exe`
  - Win32 SHA256：
    - `5cac40804d66ae28c9b16bb6a5658e3fa888dc191132f6de7af23ef5ce36e698  FMO-Dashboard-Windows-Desktop-x86-Setup-v2.0.3.exe`
  - 公开下载地址：
    - `https://fmo.bh1jss.net/downloads/v2.0.3/FMO-Dashboard-Windows-Desktop-x64-Setup-v2.0.3.exe`
    - `https://fmo.bh1jss.net/downloads/v2.0.3/FMO-Dashboard-Windows-Desktop-x86-Setup-v2.0.3.exe`
- iOS：
  - `ios/App/App.xcodeproj/project.pbxproj` 更新为 `MARKETING_VERSION 2.0.3`、`CURRENT_PROJECT_VERSION 20300`。
  - `npm run ios:sync` 通过。
  - `xcodebuild archive` 通过，归档位置：
    - `build/ios/archive/FMO-Dashboard-2.0.3-20300.xcarchive`
  - development 导出 IPA 通过：
    - `release/ios/FMO-Dashboard-iOS-Development-v2.0.3.ipa`
    - `release/ios/SHA256SUMS-ios-development-v2.0.3.txt`
  - 该 IPA 为 Apple Development 签名包，不是 TestFlight / App Store 分发包。

未完成 / 阻塞：

- Android：本机当前未连接 Android 设备，未做安装后真机冒烟测试。
- Windows：本轮公开包为 unsigned 安装包，Windows SmartScreen 可能仍会提示未知发布者。
- iOS TestFlight / App Store：需要 Apple Distribution 证书和 App Store Connect 上传流程；本轮只产出 development IPA。

#### 2026-06-28 安卓竖屏罗盘高度跳动修复

发现问题：

- 安卓版竖屏下，仪表盘“当前呼叫”区域的罗盘框在“通联状态”和“无人发言 / 等待实时事件”之间切换时，内容行数和高度缺少稳定约束，容易造成页面上下跳动。
- 同类响应式断点也可能影响其他窄屏版本，因此按通用前端样式修复，而不是只在 Android 原生层处理。

处理方案：

- `src/views/DashboardView.vue` 中主罗盘面板从 `min-height` 调整为固定 `height`，并同步收紧桌面窄宽、平板 / 手机竖屏、小屏横屏等断点里的覆盖规则。
- 罗盘文本固定为两行结构：第一行显示方位和角度，第二行显示距离；未知方位时同样保留两行空间，避免有无实时呼叫时布局高度变化。
- 罗盘区域增加 `overflow: hidden`、固定行高和省略处理，避免旧 Android WebView 字体渲染差异把卡片撑高。

#### 2026-06-28 安卓横屏状态栏 / 导航栏覆盖修复

发现问题：

- 部分 Android 手机横屏时仍显示顶部状态栏和底部系统导航栏，仪表盘没有进入真正的沉浸式全屏，顶部标题和系统图标发生遮挡。

处理方案：

- `android/app/src/main/java/net/bh1jss/fmodashboard/MainActivity.java` 增加按屏幕方向切换系统栏策略：
  - 横屏时使用 `WindowCompat.setDecorFitsSystemWindows(window, false)`、`WindowInsetsControllerCompat.hide(Type.systemBars())` 和旧式 `SYSTEM_UI_FLAG_IMMERSIVE_STICKY` 组合，隐藏状态栏与导航栏，并允许短边刘海区域布局。
  - 在 `onCreate`、`onResume`、`onWindowFocusChanged`、`onConfigurationChanged` 后重复应用，避免部分厂商 ROM / WebView 焦点恢复后把系统栏重新拉出来。
  - 竖屏时恢复系统栏显示，继续使用现有安全区 inset 逻辑。

## 当前本地注意事项

本清单创建时，工作区还存在与本次 V2.0-tofix 记录无关的既有改动：

- `docs/ios-packaging.md`
- `ios/App/App.xcodeproj/project.pbxproj`
- `docs/android-v2.0-handoff.md`
- `docs/v2.0-desktop-release-handoff.md`
- `docs/v2.0-ios-release-handoff.md`

后续提交或打包前需要再次确认这些文件是否应纳入对应任务。
